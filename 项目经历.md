### 成长记录 Growth Log — VS Code 扩展
2026.08

项目简介：一款 VS Code 扩展，将日常开发中的代码改动、问题排查与性能调优就地沉淀为结构化的面试能力证据（STAR 话术卡、成长时间线、项目经历简历）。支持 git 钩子自动抓取提交上下文、AI 自动起草反思记录、按项目聚合生成精炼简历，全链路零外部依赖（无需 Python 或任何其他工具），以 VSIX 离线包分发。

**本人负责**
- 主导全栈设计与开发，基于 TypeScript + VS Code Extension API + esbuild 构建模块化架构（13 个源文件、约 3000 行代码），从需求分析到 VSIX 离线打包全链路独立完成
- 设计 git post-commit 钩子自动抓取机制：钩子调用 capture.js 采集 commit 上下文（仓库/分支/改动文件/diff），按 commit 哈希去重、过滤 merge 提交与空 diff，实现零点击囤原始素材；钩子脚本复制到稳定路径（档案目录），扩展升级/卸载后自动自愈
- 实现 AI 自动起草管线：通过 OpenAI 兼容接口（DeepSeek / OpenAI / 通义千问 / 智谱 / Kimi）调用 LLM 从 diff 起草「问题/根因/方案/收获/STAR」六字段结构化记录，支持 5 家厂商预设 + 自定义 URL，API Key 存于 VS Code SecretStorage 不落盘
- 开发「生成简历（项目经历）」功能：按 git 仓库来源自动聚合多条记录为精炼三段式卡片（项目简介 / 本人负责 / 成果），前端 Webview 支持勾选、按项目/标签分组切换、编辑、↑↓排序、逐条移除素材、AI 聚合润色，后端导出 Markdown 可直接粘贴进简历
- 构建自包含交互式 Dashboard：从本地 JSON 数据生成离线 HTML（历程时间线 + 全文搜索 + 单项目成长轨迹 + STAR 话术卡），采用 CSP nonce 安全策略，保存记录即自动刷新，浏览器直接打开无需服务器
- 实现侧边栏 TreeView 数据层：支持按项目/时间/标签三种分组、关键词全文搜索（标题/问题/方案/收获/标签五字段）、项目/标签筛选，大分组超过 40 条自动按月二级折叠，基于文件 mtime 内存缓存避免重复读盘
- 解决关键工程阻塞：vsce 打包工具因运行环境 safe-delete shim 损坏 npm 依赖（dist/*.js 被删但 .js.map 保留）无法使用，手写纯 Node.js ZIP 打包器（CRC32 校验 + Deflate 压缩 + VSIX manifest 生成），成功产出 222KB 合规 VSIX 包并通过 ZIP 结构完整性验证

**成果**
- 13 个 TypeScript 模块、约 3000 行源码，VSIX 包 222KB，零运行时外部依赖（不含任何 Python/WorkBuddy/第三方运行时）
- 注册 12 个 VS Code 命令、5 家 AI 厂商预设接入、可自定义数据目录，全部数据本地存储不上传
- 经 9 个版本迭代（v0.9.1 → v0.9.9），每轮经 esbuild 打包 + tsc 类型检查 + node --check 语法校验 + ZIP 结构验证四重验证
- v0.9.9 完成架构重构：拆分「上帝文件」extension.ts（16KB → 5.5KB，-66%），抽取公共 webview-common 模块消除 3 处重复 nonce / 2 处重复 escapeHtml / 大段重复 CSS，新增 3 个职责单一的模块（entryView / hooks / browse）
